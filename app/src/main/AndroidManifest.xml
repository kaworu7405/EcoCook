<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ecocook">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcoCook">
        <activity
            android:name=".manage_fridge.EditIngredient"
            android:exported="true" />
        <activity
            android:name=".manage_fridge.RecipesResult2"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".manage_fridge.DetailList"
            android:exported="true" />
        <activity
            android:name=".manage_fridge.DetailLook"
            android:exported="true" />
        <activity
            android:name=".manage_fridge.RemoveIngredient"
            android:exported="true" />
        <activity
            android:name=".manage_fridge.AddIngredient"
            android:exported="true" />
        <activity
            android:name=".manage_fridge.MyFridge"
            android:exported="true" />
        <activity
            android:name=".MainMenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MemberInitActivity" />
        <activity android:name=".passwordResetActivity" />
        <activity android:name=".MyPageActivity" />
        <activity android:name=".ChangeProfileActivity" />
        <activity android:name=".PostingActivity" />
        <activity android:name=".PostingDetailActivity" />
        <activity android:name=".writePostingActivity" />
        <activity android:name=".RecipesSearch" />
        <activity android:name=".functionActivity" />
        <activity android:name=".AuthLocationActivity" />
        <activity android:name=".MyPostingActivity" />
        <activity android:name=".MyCommentActivity" />
        <activity android:name=".MyMessageActivity" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".MyRecipes" />
        <activity android:name=".manage_fridge.RecipesCheck" />
        <activity android:name=".manage_fridge.RecipesResult" />
    </application>

</manifest>